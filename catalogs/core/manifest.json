{
  "name": "core",
  "version": "0.1.0",
  "spec_version": "v1",
  "description": "LambdaCore-shaped utility packages. Catalog code calls into these helpers — `$command_utils:object_match_failed`, etc. — instead of inlining the same notify-and-bail branches per verb. Each helper class mirrors a sibling LambdaCore object (#219 command_utils, #20 string_utils, #3669 object_utils, …); their parent class `$utils` mirrors LambdaCore #288 (Generic Utilities Package). Initial release ports just the verbs we need from $command_utils; sibling packages and additional verbs land as catalog code reaches for them.",
  "license": "MIT",
  "depends": ["@local:chat"],
  "classes": [
    {
      "local_name": "$utils",
      "parent": "$thing",
      "description": "Marker base for utility singletons — mirrors LambdaCore #288 (Generic Utilities Package). Doesn't carry behavior; subclasses are the actual helper objects ($command_utils, future $string_utils / $object_utils / etc.). Wizard-owned and read-only by convention.",
      "properties": [
        { "name": "help_msg", "type": "str|list<str>", "default": "", "perms": "r" }
      ],
      "verbs": []
    },
    {
      "local_name": "$command_utils",
      "parent": "$utils",
      "description": "LambdaCore $command_utils (#219). Helpers that command verbs lean on so the same notify-and-bail branches don't get inlined into every verb body. Initial port carries only :object_match_failed; the rest of the LambdaCore surface (`:player_match_failed`, `:do_huh`, `:read`, `:yes_or_no`, `:running_out_of_time`, `:explain_syntax`, …) lands as woo verbs need them.",
      "properties": [],
      "verbs": [
        {
          "name": "object_match_failed",
          "perms": "rxd",
          "direct_callable": true,
          "skip_presence_check": true,
          "arg_spec": { "args": ["match_result", "string"] },
          "source": "verb :object_match_failed(match_result, string) rxd {\n  // LambdaCore $command_utils:object_match_failed (#219), faithfully\n  // ported. Returns true iff the match was bad (and a message was told\n  // to the actor); returns 0 (false) when match_result is a real,\n  // valid object the caller can proceed with.\n  //\n  // Divergence from LambdaCore (1): the original picks notify vs tell\n  // based on `$perm_utils:controls(caller_perms(), player)`; woo's chat\n  // surface is uniform — `tell(actor, …)` is the one durable-line API,\n  // and `actor` is the calling player by substrate convention. So the\n  // tell-vs-notify branch collapses to `tell(actor, …)`.\n  //\n  // Divergence (2): the original has a special-case for `string` that\n  // begins with `#` (a literal objref like `#-1`) so the message reads\n  // `#-1 does not exist.` instead of `I don't know which \"#-1\" you\n  // mean.`. woo's matcher returns `#`-prefixed ids only for paste-back\n  // (and the lexer strips `#` to turn them into ObjRef literals — see\n  // dsl-compiler.ts ref()), so the special-case isn't reachable from\n  // ordinary command parsing. The branch is omitted; if a future caller\n  // passes `#xxx` strings, add it back.\n  if (match_result == $nothing) {\n    tell(actor, \"You must give the name of some object.\");\n    return true;\n  }\n  let q = str_char(34);\n  if (match_result == $failed_match) {\n    tell(actor, \"I see no \" + q + string + q + \" here.\");\n    return true;\n  }\n  if (match_result == $ambiguous_match) {\n    tell(actor, \"I don't know which \" + q + string + q + \" you mean.\");\n    return true;\n  }\n  // Tombstoned, never-existed, or otherwise unresolvable id. Catalog code\n  // that takes raw ObjRef-shaped strings (e.g. paste-back of a recycled\n  // id) used to fall through here and proceed with a phantom object.\n  // Mirror LambdaCore's last `elseif (!valid(match_result))` branch.\n  if (!valid(match_result)) {\n    tell(actor, to_string(match_result) + \" does not exist.\");\n    return true;\n  }\n  return false;\n}"
        }
      ]
    },
    {
      "local_name": "$string_utils",
      "parent": "$utils",
      "description": "LambdaCore $string_utils (#20). Initial port carries only :names_of (the renderer that @contents / @kids / @parents lean on); the rest of the LambdaCore string surface (`:trim`, `:english_list`, `:abbreviated_value`, `:left`, `:right`, `:centre`, …) lands as woo verbs need them. woo's substrate already provides `str_trim`, `str_lower`, `str_starts`, `str_index`, `str_slice`, `str_char`, `str_join` as builtins, so the verb-side $string_utils helpers are the LambdaCore conveniences that compose on top.",
      "properties": [],
      "verbs": [
        {
          "name": "names_of",
          "perms": "rxd",
          "direct_callable": true,
          "skip_presence_check": true,
          "arg_spec": { "args": ["objects"] },
          "source": "verb :names_of(objects) rxd {\n  // LambdaCore $string_utils:names_of (#20). Renders a list of objects\n  // as `name1(#id1)   name2(#id2)` — three-space separator, paste-able\n  // ids per the woo `#`/`$` convention so a reader can copy any single\n  // identifier back into eval. LambdaCore emits `name(#123)` directly\n  // because every id starts with `#`; woo prefixes `#` for ULIDs and\n  // leaves corenames (`$wiz`) bare.\n  if (typeof(objects) != \"list\") {\n    return \"\";\n  }\n  let line = \"\";\n  for item in objects {\n    if (typeof(item) == \"string\" && valid(item)) {\n      let s = item;\n      if (!str_starts(s, \"$\")) { s = \"#\" + s; }\n      line = line + item.name + \"(\" + s + \")   \";\n    }\n  }\n  return str_trim(line);\n}"
        },
        {
          "name": "to_value",
          "perms": "rxd",
          "direct_callable": true,
          "skip_presence_check": true,
          "arg_spec": { "args": ["s"] },
          "source": "verb :to_value(s) rxd {\n  // LambdaCore $string_utils:to_value (#20). Best-effort parse of a\n  // user-typed value: returns [ok, value] so callers can detect parse\n  // failure without try/catch. Used by @set / @property for the\n  // `to <value>' tail.\n  //\n  // Initial port covers strings (quoted or bare), integers, floats,\n  // and objref literals (`#xxx` strips the prefix to the underlying\n  // ObjRef; `$xxx` is returned verbatim because woo stores corenames\n  // *as* the ObjRef). Lists and maps are deferred until a caller\n  // needs them; LambdaCore's full $string_utils:value_to_literal\n  // support lands incrementally.\n  if (typeof(s) != \"string\") { return [false, \"not a string\"]; }\n  let trimmed = str_trim(s);\n  let n = length(trimmed);\n  if (n == 0) { return [true, \"\"]; }\n  let first = str_slice(trimmed, 1, 1);\n  // Quoted string: strip a matched pair of double quotes.\n  let quote = str_char(34);\n  if (n >= 2 && first == quote && str_slice(trimmed, n, n) == quote) {\n    return [true, str_slice(trimmed, 2, n - 1)];\n  }\n  // Objref literal: `#xxx` evaluates to the underlying ObjRef (the\n  // dsl-compiler does the same for `#`-prefixed literals in eval).\n  // No-validation: returning the bare id lets the caller decide\n  // whether to require valid().\n  if (n >= 2 && first == \"#\") {\n    return [true, str_slice(trimmed, 2)];\n  }\n  // Corename literal: `$wiz` IS the ObjRef in woo, so the bare token\n  // works directly. Only treat as objref if the entire token is a\n  // contiguous corename (no whitespace).\n  if (first == \"$\" && str_index(trimmed, \" \") == 0) {\n    return [true, trimmed];\n  }\n  // Integer (round-trips through to_string).\n  let as_int = to_int(trimmed);\n  if (to_string(as_int) == trimmed) { return [true, as_int]; }\n  // Float (round-trips through to_string).\n  let as_float = to_float(trimmed);\n  if (to_string(as_float) == trimmed) { return [true, as_float]; }\n  // Fall back to the bare string.\n  return [true, trimmed];\n}"
        }
      ]
    },
    {
      "local_name": "$code_utils",
      "parent": "$utils",
      "description": "LambdaCore $code_utils (#153). Initial port carries :parse_propref — the helper that splits a `<obj>.<prop>` token into its parts so command verbs (@property, @set, @rmproperty, @display) don't have to. The rest of LambdaCore $code_utils (verb-introspection helpers like :find_verb_named, :explain_verb_syntax, :short_prep) lands when a woo verb reaches for them.",
      "properties": [],
      "verbs": [
        {
          "name": "parse_propref",
          "perms": "rxd",
          "direct_callable": true,
          "skip_presence_check": true,
          "arg_spec": { "args": ["string"] },
          "source": "verb :parse_propref(string) rxd {\n  // LambdaCore $code_utils:parse_propref (#153). Splits `<obj>.<prop>`\n  // (or bare `$prop` against `$system`) into [obj_str, prop_str], or\n  // returns false on parse failure. The obj_str is left as-typed so\n  // the caller does its own resolution via $match:match_object.\n  //\n  // Divergence (1): LambdaCore returns `{\"#0\", prop}` for the bare\n  // `$foo` case (treating it as `$system.foo`). woo uses `$system`\n  // verbatim — same intent, woo-shaped corename.\n  if (typeof(string) != \"string\") { return false; }\n  let s = string;\n  let dot = str_index(s, \".\");\n  if (dot > 0) {\n    let object = str_slice(s, 1, dot - 1);\n    let prop = str_slice(s, dot + 1);\n    if (object == \"\" || prop == \"\") { return false; }\n    return [object, prop];\n  }\n  if (str_starts(s, \"$\")) {\n    return [\"$system\", str_slice(s, 2)];\n  }\n  return false;\n}"
        }
      ]
    }
  ]
}
