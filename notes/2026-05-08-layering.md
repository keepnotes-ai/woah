
Tier 1 — pure superstructure, no substrate excuses (~900 lines, biggest visual win)

1. Player surface (world.ts:7127–7475) — playerWho, playerJoin, playerExamine, playerHelp, playerSessionStats, connectedPlayers, playerExamineRemote,
obviousCommandVerbs, obviousCommandSyntaxes, formatWhoDuration, formatVerbNameForExamine, matchPlayerForCommand, etc. These are pure LambdaCore
@who/@join/@examine. Bootstrap (bootstrap.ts:602–625) already labels them LambdaCore-style. They should live in a `core` catalog (or player catalog)
  as ordinary woocode. The substrate only needs to expose: connected_players(), session_stats(actor), obvious_verbs(target) as small generic primitives.
2. Help DB (world.ts:4153–4322 + 6805–6808) — entire *pass*/*forward*/*objectdoc*/*verbdoc* directive interpreter. There's already a catalogs/help/
catalog that should own this. No native helpers needed once :get_topic is a normal woocode verb walking properties.
3. Room/look composition (world.ts:6988–7125, ~140 lines) — composeRoomLook, spaceLookSelf, roomWho, lookEntryFor, titleForLook, defaultLookSelf,
chatPresent. These hardcode the room renderer. Move to bootstrap source verbs on $space/$room (or a chat catalog). Keep only describeObjects batch RPC
as a primitive — that's a real cross-host optimization.


Tier 2 — $match is already specified as a bootstrap class (~600 lines)

4. Command parser body (world.ts:7552–7950) — parseCommandMap, parseTokens, prep table, commandPatternMatches/commandPrepMatches/commandSlotMatches,
commandArgsFrom. spec/semantics/match.md (status: implemented) explicitly says $match is a bootstrap class implemented as ordinary verbs. Today it's
TypeScript hiding behind match_object/match_verb/parse_command native shims. Push the bodies into woocode on $match; keep only the cross-host
commandVerbCandidates lookup as a primitive.
match_object/match_verb/parse_command native shims. Push the bodies into woocode on $match; keep only the cross-host commandVerbCandidates lookup as a primitive.
5. Speech-prefix sugar (world.ts:7571–7622) — lowerSpeechPrefixPlan literally hardcodes /me, :, ], |, <, ", `, /tell, [style]. This is *the* most catalog-leaning piece
of code in core. Replace with a $system.speech_prefixes map (or a chat-catalog property) consulted by $match:plan_command.

Tier 3 — surfaces that are already named like catalogs (~1,000 lines)

6. Builder / Programmer / Editor (world.ts:2568–3589) — every method already takes a surfaceClass: ObjRef argument, which is an admission these are catalog objects. The
  prog catalog should own them. The substrate already has defineProperty, addVerb, setObjectName etc. as primitives; the surface methods are pure orchestration that
woocode can do. This is the biggest single subtree to move.
  => in progress

7. Guest lifecycle (world.ts:5364–5594, plus guest_on_disfunc) — allocateGuest, returnGuest, guestInventoryFallback, placeAllocatedGuest, resetGuestOnDisconnect,
rebuildGuestPool. Branches on $guest/$player parentage, hardcodes guest_N IDs. Should be a guest catalog. Aligns with your existing memory rule: core must not depend on
  bundled-catalog objects.

Tier 4 — symbolic, cheap, sets the precedent

8. Default title/look_self (bootstrap.ts:579–580 + world.ts:6612–6617) — natives whose docstring already shows the equivalent one-line woocode. Replace with
sourceVerb/bytecode. This is a five-minute change that demonstrates the direction.
  => done

9. thing_look, thing_moveto, default_title — same story. Already trivially expressible.
10. Hardcoded $-name list at world.ts:2874–2882 (recycle reserved-list) — should be $system.reserved_classes data, not a TS literal.

